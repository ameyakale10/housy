[pytest]
markers =
    firestore: integration tests against a real Firestore database (slow, needs cloud creds)
# Everyday runs stay fast + offline. Run the cloud tests with:  pytest -m firestore
addopts = -m "not firestore"
